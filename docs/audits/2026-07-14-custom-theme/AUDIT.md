# 8772 主题改造验收（2026-07-14）

## 结论

通过。终端绿预设已删除，新增酒红、浅红、绚彩，并加入可拖拽 HSV 渐变色板。自定义主题只在显式保存后持久化；取消、关闭和 Esc 恢复原主题。拖拽使用 pointer capture、`requestAnimationFrame` 与现有交互冻结机制；提交换肤时先清理五图旧 Tooltip，再错峰复用原有 ECharts 实例，避免快速滚动后的 stale-item 竞态。

## 视觉证据

- `01-before.png`：改造前主题栏基线。
- `02-red.png`：酒红深色主题。
- `03-rose.png`：浅红主题。
- `04-prismatic.png`：首轮绚彩主题。
- `05-custom-open.png`：自定义色板浮层与即时预览。
- `06-custom-saved.png`：保存后的自定义“暖霞”。
- `07-final-prismatic.png`：24px 主题点击目标与最终绚彩交付状态。
- `08-before-vs-final.png`：相同视口下改造前暖沙与最终绚彩并排比较。
- `09-extreme-white-dark.png`：深色模式选择纯白主色后的自动保护状态，正文、控件、涨跌色和保存按钮保持可读。
- `10-extreme-dark-lower-panels.png`：极端深色自定义主题下的下半页模块可读性。
- `11-extreme-dark-05b.png`：极端深色自定义主题下核心标的队列与口径区可读性。

## 真实页面验收

页面：`http://127.0.0.1:8772/market-liquidity-radar`

| 项目 | 结果 |
|---|---|
| 主题顺序 | `cloud / mist / sand / ink / slate / midnight / red / rose / prismatic / custom` |
| 终端绿删除 | 页面按钮、CSS 主题块和主题数组均不存在；旧 `terminal` 偏好映射到 `midnight` |
| 酒红 / 浅红 / 绚彩 | 逐一点击与截图通过；文字、控件、表格和图表可读 |
| 渐变色板 | 实际拖拽后 HEX 与游标连续同步；色相、名称和浅/深模式控件可用 |
| 无效输入 | `#12` 标记 `aria-invalid=true`，保存按钮禁用 |
| 保存与恢复 | “暖霞 #B4557A”保存后刷新仍恢复；切换预设后取消预览回到原主题；Esc 同样有效 |
| 3 秒刷新稳定性 | 编辑器保持打开超过 10 秒、跨 3 个刷新周期，颜色、浮层和滚动位置不跳变 |
| 可访问性 | 所有主题按钮为 24×24px；状态同步 `aria-pressed/expanded`；色板支持键盘方向键 |
| 控制台 | 页面日志为空，无 JavaScript 异常 |
| 高压交互 | 05B 悬停后 80ms 内快速滚顶并立即打开主题、修改 HEX；五图先清理旧 Tooltip，普通图与05B错峰重绘，控制台持续为空 |
| 服务隔离 | 8772 health `ok=true`、刷新间隔 3000ms；8771 runtime health 仍 `ok=true`，未重启采集服务 |

## 自动化回归

- 主项目：67 项市场流动性雷达测试通过。
- 独立开源版：73 项测试通过。
- Node VM 直接执行生产 `customThemeTokens`，对浅/深 × 白、黑、灰、黄、青、蓝及低饱和黄 14 组逐项校验：正文 ≥7:1，次要文字、强调色、活动态文字、上涨红和下跌绿均 ≥4.5:1。
- 两份前端 JavaScript 均通过语法检查。
- 独立版通过 Python 编译、启动自检与 `git diff --check`。
- 主项目与独立版 HTML/CSS/JS 三份资源逐字同步。

## 数据与视觉语义

自定义主色只改变页面背景、表面、边界、正文与交互强调。上涨/流入红、下跌/流出绿和告警黄保持固定；对白、黑、灰和低饱和高亮极端输入自动派生安全强调色。绚彩只使用低饱和静态渐变，不包含动画、霓虹或发光描边。
