# The standard-library launcher reads the process environment and deliberately
# does not parse .env automatically.
MLR_HOST=0.0.0.0
MLR_PORT=8772
# MLR_DATA_DIR=/absolute/private/path
# MLR_NO_BROWSER=1
