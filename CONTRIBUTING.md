# 参与贡献

感谢参与市场流动性雷达。请先通过 Issue 说明计划修改的用户问题、数据来源和验收方法，再提交小而清晰的 Pull Request。

## 开发原则

1. 保持本地优先：不得上传用户数据库、缓存、Cookie、Token、密钥或内网信息。
2. 保持来源透明：新增图表必须标明数据提供方、接口/字段、观察时间、刷新频率和降级口径。
3. 不伪造精度：区分原生资金流、本机观察值、估算代理值和缺失数据。
4. 遵守交易时段：集合竞价、连续竞价、午间休市和收盘后的时间轴必须明确。
5. 保持跨平台：路径和启动器需在 Windows、macOS 上可用，避免只支持单一 Shell。
6. 保持前端流畅：盘中刷新不应打断拖拽、缩放、悬停和模块布局。

## 本地验证

项目要求 Python 3.9 或更高版本。单元测试和自检不需要网络：

```bash
python3 -m unittest discover -s tests -v
python3 -m compileall -q start.py src tests
python3 start.py --self-check
python3 start.py --print-urls-only
```

若新增第三方依赖，请锁定合理的最低版本，说明用途，并确保启动失败时有中文可操作提示。测试不得依赖真实交易日、特定局域网 IP 或第三方接口永远在线。

## Pull Request 清单

- [ ] 没有提交数据库、缓存、日志、Cookie、密钥或真实用户信息。
- [ ] 每个新增数据字段都有来源与口径说明。
- [ ] 新功能包含单元测试或可重复的手工验收步骤。
- [ ] Windows/macOS 启动路径未被破坏。
- [ ] README、风险声明和截图已按需更新。
