# 安全政策

## 报告漏洞

请不要在公开 Issue 中披露可被直接利用的漏洞、访问令牌、Cookie、内网地址或真实用户数据。请通过仓库维护者在 GitHub Security 页面启用的“私密漏洞报告”渠道提交；正式发布前，维护者应在仓库设置中启用该功能并补充安全联系邮箱。

报告应包含受影响版本、复现步骤、预期影响和建议修复。维护者会尽快确认，但当前项目不承诺固定的响应时限。

## 部署边界

- 默认监听 `0.0.0.0` 是为了可信局域网访问，不代表可安全公网部署。
- 当前没有身份认证、TLS、访问审计或请求限流。禁止路由器端口转发和公网暴露。
- 在公共 Wi-Fi、云服务器或不可信网络上，应使用 `--host 127.0.0.1`，并通过受控的反向代理补齐认证和 TLS。
- 不要把行情账户、券商账户、Cookie、Token 或密钥写入源码、网页或 Git 历史。
- 本地数据默认存放在被 Git 忽略的 `data/`；提交前仍应运行 `git status` 并人工审查。

## 第三方数据

不得绕过登录、验证码、付费墙、访问控制或服务方的技术限制。第三方接口变化造成的数据错误也属于运行风险，应在 UI 中显著展示来源、时间、缓存和降级状态。
